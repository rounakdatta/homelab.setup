---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Only include actual ingress resources, not templates
resources:
  - nginx-example.yaml
  # Add more ingress files here as you create them
  # - another-service-ingress.yaml
