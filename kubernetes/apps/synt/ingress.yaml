---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: synt-ingress
  namespace: apps
  annotations:
    # Automatically provision TLS certificate with Let's Encrypt
    cert-manager.io/cluster-issuer: "letsencrypt-prod"

    # Force HTTPS redirect
    traefik.ingress.kubernetes.io/redirect-entry-point: https

    # Security headers
    traefik.ingress.kubernetes.io/custom-response-headers: |
      X-Frame-Options: SAMEORIGIN
      X-Content-Type-Options: nosniff

    # Authelia authentication
    traefik.ingress.kubernetes.io/router.middlewares: authelia-authelia@kubernetescrd

spec:
  tls:
  - hosts:
    - synt.taptappers.club
    secretName: synt-tls-secret

  rules:
  - host: synt.taptappers.club
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: synt
            port:
              number: 8080
