apiVersion: v1
kind: Service
metadata:
  name: {{ include "zepp-life-mcp.fullname" . }}
  labels:
    {{- include "zepp-life-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zepp-life-mcp.selectorLabels" . | nindent 4 }}
